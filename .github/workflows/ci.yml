name: .NET Core

on:
  push:
    branches: [ main ]
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches: [ main ]

permissions:
  contents: read

jobs:
  build:
    permissions:
      actions: read
      contents: write
      deployments: read
      pages: write
      id-token: write
    uses: dpvreony/.github/.github/workflows/dotnet-ci.yml@dee25ce4a8083e0c4b334e8e71d01f751e57227b
    with:
      solutionName: "Dpvlab.Spark"
