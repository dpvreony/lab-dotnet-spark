name: .NET Core

on:
  push:
    branches: [ main ]
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches: [ main ]

permissions:
  contents: read

jobs:
  build:
    permissions:
      actions: read
      contents: write
      deployments: read
      pages: write
      id-token: write
    uses: dpvreony/github-action-workflows/.github/workflows/dotnet-ci.yml@f890ded2a3c79fb41ef378fd22220a93d2f734f8
    with:
      solutionName: "Dpvlab.Spark"
