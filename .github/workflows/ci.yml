name: .NET Core

on:
  push:
    branches: [ main ]
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches: [ main ]

permissions:
  contents: read

jobs:
  build:
    permissions:
      actions: read
      contents: write
      deployments: read
      pages: write
      id-token: write
      pull-requests: write
    uses: dpvreony/github-action-workflows/.github/workflows/dotnet-ci.yml@465c0dea4f14ee1ceb8df645183e5d67d6132428
    with:
      solutionName: "Dpvlab.Spark"
