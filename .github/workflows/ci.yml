name: .NET Core

on:
  push:
    branches: [ main ]
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches: [ main ]

permissions:
  contents: read

jobs:
  build:
    permissions:
      actions: read
      contents: write
      deployments: read
      pages: write
      id-token: write
      pull-requests: write
    uses: dpvreony/github-action-workflows/.github/workflows/dotnet-ci.yml@636a4e16425b842361d95ae2c79b3a846e01347f
    with:
      solutionName: "Dpvlab.Spark"
