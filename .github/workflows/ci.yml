name: .NET Core

on:
  push:
    branches: [ main ]
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches: [ main ]

permissions:
  contents: read

jobs:
  build:
    permissions:
      actions: read
      contents: write
      deployments: read
      pages: write
      id-token: write
      pull-requests: write
    uses: dpvreony/github-action-workflows/.github/workflows/dotnet-ci.yml@6d33c5b8261cc2fa7b090322716b9696bf6aeffe
    with:
      solutionName: "Dpvlab.Spark"
